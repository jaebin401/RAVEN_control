# Characterization results

This tracked directory contains selected RAVEN hardware-test bundles transferred
through Git. Scratch runs belong in the ignored `logs/characterization/`
directory.

Each committed run must contain `metadata.yaml`, `run_status.yaml`,
`test_plan.yaml`, configuration snapshots, `notes.md`, `raw.csv`, and the
generated `summary.yaml`.

